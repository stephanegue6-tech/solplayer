fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
pydantic==2.9.2
pydantic[email]==2.9.2
python-dotenv==1.0.1

# Pour brancher PostgreSQL (recommandé en production, cf. cahier des
# charges section 4.2) au lieu du SQLite par défaut — nécessaire pour
# docker-compose.yml :
psycopg2-binary==2.9.10

# Authentification / RBAC (cf. cahier des charges 4.3)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9   # requis par FastAPI pour le formulaire OAuth2PasswordRequestForm (/auth/login)

# Migrations de schéma
alembic==1.13.2

# Export de rapports (CSV natif via stdlib) et PDF (cahier des charges 3.1,
# 3.2, "Interopérabilité")
reportlab==4.2.2

# Anti brute-force sur /auth/login + limitation globale (cahier des charges
# 4.3 / bonnes pratiques sécurité API)
slowapi==0.1.9

# Détection + lecture de plaque sur image (Module 4, cahier des charges 3.4)
# — pipeline local, voir app/anpr_engine.py. opencv-python-headless (pas
# opencv-python) : pas de dépendances GUI inutiles dans un conteneur serveur.
# Le binaire `tesseract-ocr` doit être installé au niveau système (voir
# Dockerfile) ; pytesseract n'en est qu'un wrapper Python.
opencv-python-headless==4.10.0.84
pytesseract==0.3.13
numpy==1.26.4
